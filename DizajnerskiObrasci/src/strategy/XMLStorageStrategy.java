package strategy;

import mvc.DrawingModel;

public class XMLStorageStrategy implements DrawingStorageStrategy {
    
    public static void saveDrawing(DrawingModel drawingmodel, String filePath) {
        // Implementirajte logiku za serijalizaciju crteÅ¾a u XML format i Ä?uvanje u datoteku
    	
    	/*serijalizacija crteza u XML format i cuvanje u fajl*/
    }
}
